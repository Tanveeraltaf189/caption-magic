import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.captionmagic.app',
  appName: 'caption-magic# Prompts:# App name: caption-magic# Package ID: com.captionmagic.app# Web asset directory: dist (default)',
  webDir: 'dist'
};

export default config;
